{
  "name": "tiger-simulator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "publish": "npm run build && rm -rf ../../site/static/tiger-simulator && mkdir -p ../../site/static/tiger-simulator && cp -R dist/. ../../site/static/tiger-simulator/"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
