# Worm

Single-file HTML5 worm game. Source of truth lives here; publish by copying to `site/static/worm/`.

```bash
cp index.html ../../site/static/worm/index.html
```

Includes Pink Horse Arcade controls shim (`/arcade/controls.js`) with native touch UI retained (`disableTouch: true`).
