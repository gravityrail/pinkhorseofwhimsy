{
  "name": "bean-simulator",
  "version": "0.1.0",
  "description": "A first-person 3D game featuring Little Bean the dog",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build --base=/bean-simulator/",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit",
    "test": "node test.js",
    "publish": "npm run build && rm -rf ../../site/static/bean-simulator && mkdir -p ../../site/static/bean-simulator && cp -R dist/. ../../site/static/bean-simulator/"
  },
  "dependencies": {
    "@babylonjs/core": "^7.36.0",
    "@babylonjs/gui": "^7.36.0",
    "@babylonjs/inspector": "^7.36.0",
    "@babylonjs/loaders": "^7.36.0",
    "@babylonjs/materials": "^7.36.0"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^1.1.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
